/*const fs=require('fs');
const data=fs.readFile("text.txt",function(err){
    if(err){
        console.log(err);
    }
});
console.log(data);
*/
const fs=require('fs');
fs.writeFile("test.txt","hi priya how are u");